import { createBrowserRouter } from 'react-router-dom';
import routes from './AppRoutes';

// Cria o router usando as rotas definidas em AppRoutes
const router = createBrowserRouter(routes);

export default router;
